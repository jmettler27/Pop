{
  "firestore": {
    "rules": "firebase-config/firestore.prod.rules",
    "indexes": "firebase-config/firestore.indexes.json"
  },
  "database": {
    "rules": "firebase-config/database.prod.rules.json"
  },
  "storage": {
    "rules": "firebase-config/storage.prod.rules"
  }
}
