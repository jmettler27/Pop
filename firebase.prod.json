{
  "firestore": {
    "rules": "firestore.prod.rules",
    "indexes": "firestore.indexes.json"
  },
  "database": {
    "rules": "database.prod.rules.json"
  },
  "storage": {
    "rules": "storage.prod.rules"
  }
}
